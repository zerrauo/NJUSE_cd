# 编程智能体项目要求与开发检查清单

> 来源：`推免考核题目学生版.pdf`。本文件是项目开发、规划、提交和演示时的约束基线；每次新增功能或调整方案前，先核对本文件。

## 项目目标

独立设计并实现一个编程智能体（Coding Agent）。它通过与大语言模型交互，能够自主读写本地文件、执行命令，并完成用户交给它的编程任务。

## 硬性限制

- 不得在现成 Agent 产品上封装界面或二次包装。
- 不得使用任何 Agent 框架或 SDK，例如 LangChain、LlamaIndex、OpenAI Agents SDK、Claude Agent SDK、AutoGen、CrewAI 等。
- 可以使用模型厂商 API 客户端库、OpenAI 兼容网关，以及模型原生的 Tool Calling 接口。
- 不得依赖 API 服务端托管的代码执行或文件工具，例如 Code Interpreter、Files API。
- Agent 的重要逻辑必须自行实现，至少包括：
  - 对话历史与上下文管理；
  - 工具定义及本地执行；
  - 模型输出或工具调用的解析；
  - Agent 循环的终止条件；
  - 错误处理。
- API Key 和其他凭据只能通过环境变量或未纳入版本控制的配置文件提供；不得出现在仓库、`README.txt` 或演示视频中。

## 截止与仓库要求

- 截止时间：2026 年 9 月 2 日 24:00（北京时间，即 9 月 3 日 00:00）。
- 使用题目发布后新建的公开 Git 仓库（GitHub 或 Gitee）。
- 保留完整提交历史，不得压缩或改写已推送历史。
- 截止时间后不得再向仓库推送新提交。
- 评委会会结合提交时间和内容了解开发过程，因此应按阶段做清晰、真实的提交。

## 提交物

1. 公开 Git 仓库；仓库地址写入 `README.txt`。
2. `README.txt`（1000 汉字以内）：仓库地址、运行方式、特色功能、其他说明。
3. MP4 视频：2 分钟以内、200 MB 以内，演示 Agent 完成一个真实编程任务，并简要讲解功能实现；允许剪辑和加速。

最终只提交一个以本人姓名命名的 ZIP，压缩包内含 `README.txt` 和视频文件；提交平台以题目 PDF 提供的链接为准，可重复提交，以最后一次为准。

## 面试关注点

现场播放视频后，需要介绍设计方案并回答提问。重点是能解释并辩护以下问题：

- Agent 为什么按当前流程运转；
- 上下文如何保存、裁剪或压缩；
- 工具如何定义、校验与在本地执行；
- 模型工具调用如何解析并把结果反馈给模型；
- 如何判定任务完成、何时停止循环；
- 遇到 API、工具和命令失败时如何处理；
- 安全边界如何设定，例如工作目录限制、危险命令确认、凭据保护。

## 每次规划或实现前的快速核对

- [ ] 该功能是否仍是独立实现，而非包装现成 Agent 或引入禁止的 Agent 框架？
- [ ] 核心逻辑是否在本地可见、可解释、可测试？
- [ ] 是否避免了服务端托管的文件或代码执行能力？
- [ ] 是否会把密钥或敏感内容写入代码、日志、文档或视频？
- [ ] 是否有明确的停止条件、错误反馈和资源限制？
- [ ] 新增复杂度是否值得，能否在两分钟视频内清楚演示？
- [ ] 这个设计能否在面试中说明取舍和理由？
